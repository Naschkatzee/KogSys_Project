The second seed-based evaluation of the preprocessed joint-training configuration achieved a best validation accuracy of approximately 82.00%, which was obtained at epoch 286. Similar to the first seed, the model exhibited a stable and gradual learning trajectory throughout training. Validation accuracy improved rapidly during the initial epochs, surpassing 80% within the first thirty epochs, before transitioning into a slower optimization phase characterized by incremental gains. The best performance was achieved relatively late in training, indicating that the model continued to benefit from the full 300-epoch schedule and had not fully converged during the earlier stages of optimization.

Compared to Seed 0, the overall performance remained consistent. The best validation accuracy differed by less than half a percentage point, suggesting that the replicated implementation is relatively robust with respect to random initialization. The training dynamics also followed a similar pattern, with an initial phase of rapid improvement followed by a prolonged plateau and occasional small gains in later epochs. This consistency indicates that the observed performance level is not dependent on a particularly favorable seed and is instead representative of the behavior of the replicated model.

Although Seed 1 achieved a slightly lower peak performance than Seed 0, the difference is small enough to be attributed to normal stochastic variation during optimization. Importantly, the result remains within the same performance range and therefore reinforces the conclusion that the gap between the replication results and the values reported in the original publication is unlikely to be explained solely by random initialization effects.

Overall, the second seed experiment further supports the reliability of the preprocessed joint-training pipeline. Together with the first seed, it suggests that the replicated model consistently converges to a performance level around 82% accuracy, while preserving the improvements previously observed for the more challenging distributed configurations. The remaining seed runs will allow the computation of the mean accuracy and standard deviation, providing a statistically more robust estimate of the model's expected performance.
